spring:
  application:
    name: eureka-server

server:
  port: 1001

eureka:
  instance:
    hostname: localhost
  client:
    #应用为服务注册中心，不需要注册到自身
    register-with-eureka: false
    fetch-registry: false